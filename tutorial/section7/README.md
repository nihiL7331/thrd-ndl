# Layer 7 - Porting

## Build & run demo commands
```bash
cmake -B build
cmake --build build
./build/demo
```

## Expected output
```
starting an infinite loop
polite thread running
polite thread running
polite thread running
done
```
